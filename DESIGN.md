# Design System

## Experience

这是一张从顶部一路展开的学习长卷。首屏只解决“从哪里开始”，正文以八个全宽章节带形成节奏，知识卡提供渐进展开，深色互动工具穿插在对应知识附近，页面末尾以深色复习中心收束。

## Color

使用多角色轻量配色，不让任何单一色系统治页面：

- Canvas: `oklch(0.975 0.006 170)`，轻微偏薄荷的近白背景。
- Paper: `oklch(0.995 0.003 70)`，阅读卡表面。
- Ink: `oklch(0.285 0.035 255)`，正文与深色工具背景。
- Coral: `oklch(0.65 0.185 10)`，主行动和关键标记。
- Mint: `oklch(0.66 0.13 170)`，正确、掌握与复习。
- Blue: `oklch(0.67 0.13 245)`，说明和中性信息。
- Yellow: `oklch(0.78 0.13 82)`，互动提示与章节测验。

正文颜色满足至少 4.5:1，对比接近边界时优先加深文字。

## Typography

使用系统中文无衬线字体 `Microsoft YaHei`, `PingFang SC`, sans-serif。正文行高 1.7-1.9，阅读列控制在约 760px。页面主标题 52px，章节标题 36px，知识卡标题 23px；移动端分别降至 38px、29px，并允许自然换行。字间距固定为 0。

## Shape And Elevation

卡片和工具最大 8px 圆角。阅读卡只用低扩散阴影形成纸张层次；控件主要依靠实色、浅色背景与焦点环。页面区段不做浮动卡，卡片内部不再嵌套装饰卡。

## Components

- Header: 固定顶部，展示当前章和章节进度；目录按钮为辅助入口。
- Learning unit: 结论、小白解释始终可见；五层内容用语义化展开按钮控制。
- Quick check: 知识卡底部的浅绿色整宽带，选择后显示文字、图标和解释。
- Chapter interaction: 深色、单一目标的真实工具，结果区域短暂进入后保持稳定。
- Chapter quiz: 默认收起，开始后一次显示一题，提交后立即解释。
- Review center: 页面底部全宽深色区域，按今天、明天、本周、以后组织复习项。

## Motion

只使用 140-300ms 的单次 ease-out 位移、进度条或展开反馈。不允许无限动画、装饰旋转、弹性或抖动。内容默认可见，不依赖进入动画解锁。`prefers-reduced-motion: reduce` 下把动画和过渡压缩为近乎即时。

## Responsive Behavior

桌面显示右侧章节进度点和多列互动控件。900px 以下隐藏进度点，多列工具改为一至两列；390px 仍保留至少 12px 页面边距、44px 触控目标和自然换行。固定头部不能遮住锚点标题。
